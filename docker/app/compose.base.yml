#
#  Base configuration
#

services:
  app:
    # image: habidat/directloan:${DOCKER_IMAGE_TAG:?}
    restart: unless-stopped
    depends_on:
      - postgres
    labels:
      - traefik.enable=true
      - traefik.http.routers.cfe-${DOCKER_IMAGE_TAG:?}.rule=Host(`${TRAEFIK_HOST:?}`)
      - traefik.http.routers.cfe-${DOCKER_IMAGE_TAG:?}.entrypoints=websecure
      - traefik.http.services.cfe-${DOCKER_IMAGE_TAG:?}.loadbalancer.server.port=3000
    environment:
      - TZ=Europe/Berlin
      - NEXT_PUBLIC_SOLILOAN_PROJECT_NAME=${SOLILOAN_PROJECT_NAME:?}
    networks:
      - traefik_network
      - backend_network

networks:
  backend_network:
    external: true
    name: soliloan_backend_network
